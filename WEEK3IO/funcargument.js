/*function sum(a, b){
    return a + b;
}
function sub(a,b){
    return a- b;
}
function doArithmetic(a,b,fn){
    return fn(a,b);
}
const ans1= doArithmetic(1,5, sum);
const ans2= doArithmetic(10,5, sub);
 console.log(ans1); */
 //ASYNCHRONOUS OPERATION
/*const fs = require("fs");
function fileReadCallback(err, contents){ // here err is a parameters it's using because a.txt file might contain error so if it's then it'll throw error 
    console.log(contents);
}
 fs.readFile("a.txt", "utf-8", fileReadCallback); // this callback will return their output later
 
 let s = 0;
 for(let i = 0;i < 1000000; i++){
    s+=i;
 }
 console.log(s);*/
 