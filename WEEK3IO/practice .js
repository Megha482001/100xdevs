/*function sum(a,b){
    return a+b;
}
function sub(a,b){
    return a-b;
}
function doArithmatic(a,b,fn){
    return fn(a,b);
}
const ans1 = doArithmatic(5,5,sum);
const ans2 = doArithmatic(10,6,sub);
 console.log(ans1); */
 
 /*
 const fs = require("fs") // import node.js file system module to read/write the file
 const contents = fs.readFileSync("a.txt", "utf-8") // telling the fs module to read the a.txt file and store it to the contents and utf-8 is text encoding language telling js how to read the characters
 console.log(contents);
 */
/*
const fs = require("fs");

function fileReadCallback(err, contents){
    console.log(contents);
}
fs.readFile("a.txt","utf-8", fileReadCallback);
 
let s = 0;
for(let i = 0; i < 1000000000; i++){
    s+=i;
}
console.log(s);
*/